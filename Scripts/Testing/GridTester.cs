using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GridTester : MonoBehaviour
{
    TileMap3D Map;
    // Start is called before the first frame update
    void Start()
    {

    }

    public void BuildTest()
    {
        return;
        //Map = GetComponent<TileMap3D>();
        //Map.UpdateTile(5, 5, TestBuild);

        //Map.UpdateTile(4, 4, TestBuild);
        //Map.UpdateTile(4, 5, TestBuild);
        //Map.UpdateTile(4, 6, TestBuild);

        //Map.UpdateTile(5, 4, TestBuild);
        //Map.UpdateTile(5, 6, TestBuild);

        //Map.UpdateTile(6, 4, TestBuild);
        //Map.UpdateTile(6, 5, TestBuild);
        //Map.UpdateTile(6, 6, TestBuild);

    }

   
}
